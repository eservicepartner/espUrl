namespace espurl.explorer
{
   public static class ServiceHelper
   {
      public static void GetUrl(string fileName)
      {
/*         string espUrlBase = "http://espurl.me/"; //todo get espUrl
         string espUrl = null;

         if (!string.IsNullOrEmpty(espUrl))
         {
            Clipboard.SetDataObject(espUrl);
            MessageBox.Show("URL Copied into the clipboard.\n\rURL: " + espUrl, "URL Copied into the clipboard.");
         }
         else
         {
            MessageBox.Show("Something wrong espUrl is empty.", "espUrl is empty");
         }*/
      }
   }
}
